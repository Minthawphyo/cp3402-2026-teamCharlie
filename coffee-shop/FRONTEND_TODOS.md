# Frontend notes

This is a **frontend-only** brochure site. No backend is required for v1.

## Optional later (WordPress)

Only if the team moves this UI into WordPress:

1. **Contact form** — connect `Contact.jsx` so messages actually send (WP form plugin or email).
2. **Map** — replace the map placeholder on Contact with a Google Maps embed.

Everything else (menu, hours, copy, images) can stay as static frontend content for now.
